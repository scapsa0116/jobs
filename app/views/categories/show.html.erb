
<h1><%= @category.name %> - <%= pluralize(@category.jobs.count, "jobs") %></h1>

<ol>
    <% @category.jobs.each do  |job| %>
      <li><%= link_to job.title, job_path(job), class:"btn btn-default"  %></li>
    <% end  %>
</ol>
