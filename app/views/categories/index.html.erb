
category.index

Search 



    <h1>Name</h1>
    


    <ul>
  <% @categories.each do |category| %>
    <li>
      <%= link_to category.name, category_path(category) %>
      
      <ol>
        <% category.jobs.each do  |job| %>
          <li><%= link_to job.title, jobs_path  %></li>
        <% end  %>
      </ol>
    </li>
  <% end %>
</ul>

    </table>
    
